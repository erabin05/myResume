import { TictacDirective } from './tictac.directive';

describe('TictacDirective', () => {
  it('should create an instance', () => {
    const directive = new TictacDirective();
    expect(directive).toBeTruthy();
  });
});
