import { BigBookPictureDirective } from './big-book-picture.directive';

describe('BigBookPictureDirective', () => {
  it('should create an instance', () => {
    const directive = new BigBookPictureDirective();
    expect(directive).toBeTruthy();
  });
});
