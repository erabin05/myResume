import { TimeLineDirective } from './time-line.directive';

describe('TimeLineDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeLineDirective();
    expect(directive).toBeTruthy();
  });
});
